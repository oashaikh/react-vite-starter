import { Counter } from "./components/Counter";

export function App() {
  return (
    <main>
      <h1>React + Vite Starter</h1>
      <p>Replace this with your app. The Counter below is here so the e2e and unit tests have something concrete to assert on.</p>
      <Counter />
    </main>
  );
}
